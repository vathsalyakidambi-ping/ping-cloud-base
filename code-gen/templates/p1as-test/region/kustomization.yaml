kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

# NOTE: remove empty list '[]' if adding resources to this file. Empty list must exist in kustomize 5.1+ if no resources are referenced.
resources: []

configMapGenerator:
  - name: test-restart-strategy
    literals:
      - RESTART_STRATEGY=""